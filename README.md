# Simple Demo Page

This repository contains a single HTML page showing a minimal example of a web page written in Polish.

## How to open

Just open `index.html` in your web browser. You can double-click the file or run `open index.html` on macOS, `xdg-open index.html` on Linux, or use your browser's *File > Open* dialog.
